<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8" />
  <title>{{story.title}} - Web Đọc Truyện</title>
  <link rel="stylesheet" href="/assets/css/style.css" />
</head>
<body>
  <header>
    <a href="/">← Trang chủ</a>
    <h1>{{story.title}}</h1>
    <p>Tác giả: {{story.author_info.display_name}}</p>
    <p>Thể loại: {{story.category}}</p>
  </header>

  <main>
    <img src="{{story.coverUrl}}" alt="{{story.title}}" class="story-cover" />
    <p class="story-desc">{{story.description}}</p>

    <h3>📖 Danh sách chương</h3>
    <ul class="chapter-list">
      {{#each chaps }}
        <li>
          <a href="/chapter/{{this._id}}">
          Chương {{this.number}}: {{this.title}}</a>
          {{#if this.isVip}}
            <span class="vip-tag">VIP</span>
          {{/if}}
        </li>
      {{/each}}
    </ul>
  </main>
</body>
</html>
